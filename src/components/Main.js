import React, { Component } from 'react';

import Catalog from './Catalog';

class Main extends Component {
    render() {
        return (
            <main className="main">
                <Catalog />
            </main>
        );
    }
}

export default Main;